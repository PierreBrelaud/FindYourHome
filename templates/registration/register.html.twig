{% extends "front/layout.html.twig" %}
{% block body %}
<form method="post">
<div class="container">
    <div class="mt-5">
        <div class="row">
            <div class="form-group col-6">
                {{ form_label(user.username, 'Username :') }}
                {{ form_widget(user.username ) }}
                {{ form_errors(user.username) }}
            </div>

        </div>
        {{ form_end(user) }}
    </div>
</div>
</form>
{% endblock %}
